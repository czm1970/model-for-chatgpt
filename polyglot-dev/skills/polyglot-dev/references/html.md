# HTML 与页面

- 先确认是静态页面、服务端模板还是组件框架；在原有结构中修改，不把简单页面迁移为 SPA。
- 使用语义化元素、可访问名称、关联 label 和键盘可操作控件；交互还需读取 JavaScript 参考。视觉设计同时关注焦点、空态、错误态和长文本。
- CSS 沿用现有样式方案，兼顾窄屏和宽屏，避免固定宽度导致溢出。图片提供适当的替代文本并考虑尺寸占位。
- 输出不可信内容使用框架转义或安全文本接口；避免直接插入未净化的 HTML。
- 有浏览器工具时检查实际渲染、控制台、资源加载、主要交互及移动宽度。没有浏览器时报告仅完成静态检查，不声称已验证视觉效果。
